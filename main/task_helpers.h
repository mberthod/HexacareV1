/* task_helpers.h — tâches optionnelles par mode de build (capture, MFCC, mesh bench) */
#pragma once

void task_capture_start(void);
void task_mfcc_debug_start(void);
void task_mesh_bench_start(void);
